from setuptools import setup

setup(
   name='exercises',
   description='A module with exercises',
   packages=['exercises']
)
setup(
   name='gui',
   description='A gui module',
   packages=['gui']
)
setup(
   name='notes',
   description='A notes module',
   packages=['notes']
)
setup(
   name='synthesis',
   description='A synthesis module',
   packages=['synthesis']
)
